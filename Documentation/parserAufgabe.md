 fast parser, combinator parser, regex parser -> jeweils für fen und pgn , gamestate to json
 -> pgn with forward and backwards navigation

User test scenarios: downloads puzzle stuations from web as fen 
mehr tests lesen als code.

https kommt mit ever 
